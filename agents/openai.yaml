interface:
  display_name: "Frontend Prototype Planner"
  short_description: "Frontend plans, task cards, and test blueprints."
  default_prompt: "Use $frontend-prototype-planner to turn this frontend requirement into an implementation-ready, verifiable construction plan."
policy:
  allow_implicit_invocation: true
